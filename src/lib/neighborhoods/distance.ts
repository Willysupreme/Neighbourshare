// Relocated to src/domain/verification/rules.ts as part of the rebuild's
// domain-first restructuring (REBUILD_DOCUMENTATION Phase 6). This file
// is kept as a re-export shim so no import site elsewhere in the app
// needs to change.
export * from "@/domain/verification/rules";

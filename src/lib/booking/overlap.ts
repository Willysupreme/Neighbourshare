// Relocated to src/domain/bookings/overlap.ts as part of the rebuild's
// domain-first restructuring (REBUILD_DOCUMENTATION Phase 12). Kept as a
// re-export shim so no import site elsewhere in the app needs to change.
export * from "@/domain/bookings/overlap";
